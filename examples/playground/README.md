# deck.gl Community Playground

This is a standalone example of the installable `@deck.gl-community/playground` package. Edit the JSON on the left to update a live deck.gl `ScatterplotLayer` preview.

```bash
yarn
yarn start
```

To run against the local workspace packages, use `yarn start-local` from the repository root after installing dependencies.
