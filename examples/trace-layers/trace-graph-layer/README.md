# TraceGraphLayer Example

This layers-only example mounts `DeckGL` directly, adds a caller-owned legend band and time-axis
grid, then renders one normalized `TraceGraph` through `TraceGraphLayer`. It is the custom
deck-shell counterpart to the fuller `DeckTraceGraph` Tracevis example.
