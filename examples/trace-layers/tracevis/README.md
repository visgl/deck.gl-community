# Tracevis Placeholder

This private workspace reserves the final `examples/trace-layers/tracevis` path for the Tracevis
reference application. The scaffold only verifies that `@deck.gl-community/trace-layers` resolves
from an example workspace.
