import {GalleryTiles} from '../components';
import manifest from '../../../examples/gallery/manifest.json';

# Scripting Gallery

Standalone HTML demos built from scripting examples. These tiles link directly to the generated files under
`/gallery` in the website build output.

<GalleryTiles demos={manifest} />

